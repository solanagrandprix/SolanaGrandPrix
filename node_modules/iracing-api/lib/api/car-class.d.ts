import { API } from './api';
/**
 * Provides methods for interacting with car class endpoints.
 */
export declare class CarClassAPI extends API {
    /**
     * Get a list of all available car classes.
     *
     * @returns A promise resolving to an array of car class objects, or undefined on error.
     */
    getCarClasses: () => Promise<{
        name: string;
        custId: number;
        rainEnabled: boolean;
        carClassId: number;
        carsInClass: {
            carDirpath: string;
            carId: number;
            rainEnabled: boolean;
            retired: boolean;
        }[];
        relativeSpeed: number;
        shortName: string;
    }[] | undefined>;
}
